// Persisted editor poses — overwritten by save-server.js on every editor Save.
// `null` means "no saved poses yet; fall back to moves.js / localStorage".
// This file IS committed to git, so saved poses deploy to the live site.
window.SAVED_POSES = null;
